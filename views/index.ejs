
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="game" type="text" class="ghost-input" placeholder="Enter a Game" required>
          <input type="submit" class="ghost-button" value="Get Price">
        </form>
        <% if(game !== null){ %>
            <table class='gameTable'>
            <% for(var i=0; i < game.length; i++) { %>
               <tr>
                 <td>
                    <a href="/<%= game[i].url_name%>" class = 'game_link_button' role = 'button'>
                        <%= game[i].name%>
                    </a>
                 </td>
               </tr>
            <% } %>
            </table>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
