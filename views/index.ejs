
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Price of Games</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://fonts.googleapis.com/css?family=Dorsa" rel="stylesheet"> 
  </head>
  <body>
    <div class= "titleContainer">
        <a class= "mainTitle" href= "/">Price of Games</a>
    </div>
    <div class="container">
      <fieldset>
        <form class= "example" action="/" method="post" >
          <!-- <input name="game" type="text" class="ghost-input" placeholder="Enter a Game" required>
          <input type="submit" class="ghost-button" value="Get Price"> -->
          <input name="game" type="text" placeholder="Search.." required>
          <button type="submit"><i class="fa fa-search"></i></button>
        </form>
        <% if(game !== null){ %>
            <table class='gameTable'>
            <% for(var i=0; i < game.length; i++) { %>
               <tr>
                 <td>
                    <a href="/<%= urls[i]%>" class = 'game_link_button' role = 'button'>
                        <%= game[i]%>
                    </a>
                 </td>
               </tr>
            <% } %>
            </table>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
